# MoonRead Moved

MoonRead is no longer owned by the `Deep Sea Embers` novel folder.

Canonical app path:

```powershell
D:\Fogust\Workspace\Novel\MoonRead
```

Use:

```powershell
cd "D:\Fogust\Workspace\Novel\MoonRead"
npm.cmd run generate:chapters
npm.cmd run lint
npm.cmd run build
npm.cmd run smoke
```

This folder remains only as a compatibility stub for older references.
